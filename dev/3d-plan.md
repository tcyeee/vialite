# 3D 键盘预览重建计划

目标：把 `src/components/connect/KeyboardLayout3D.tsx` 从「能看出几何位置对不对」的验证级渲染，
提升到「像一块真键盘」的展示级渲染，同时解决现有的性能和颜色耦合问题。

组件已接入侧边栏「3D 预览 (Beta)」页（倒数第二项），纯展示，无交互功能。

---

## 一、设计前提

### 键帽一律等高（DSA / XDA 风格）

**不做** R1–R4 雕刻高度差。客制化配列里 KLE 的 y 坐标推不出「行」的语义：

- ortholinear 没有行的概念；
- 分体 / 列错位（columnar stagger）中同一 y 值可能横跨完全不同的手指位置；
- Alice 这类配列整体是斜的。

硬套行高差会渲染出一块歪的键盘，比等高更难看。且等高并非妥协——DSA/XDA 在 ortho、
分体、小配列上本就是主流选择，渲染成等高是如实反映。

这个前提带来三处简化：

1. 键帽形状只由 `(width, height)` 决定 → 几何体缓存 key 就是这两个数；
2. 无 `rotation.x` 倾角，键帽全部平放；
3. legend 平面的 Y 是所有键统一的常数。

### 键帽尺寸按需缓存，不预先枚举

常见宽度（1 / 1.25 / 1.5 / 1.75 / 2 / 2.25 / 2.75 / 6.25 / 7）确实有限，但枚举跑不全：
小众配列会出现 1.5u 高的数字键盘 Enter、3u 空格，以及 KLE 里手写的任意值。预先枚举仍需
「表里没有」的兜底分支，等于按需缓存外加一份要维护的清单。按需缓存拿到同样的复用率。

缓存 key 需量化（`q()`，三位小数）：`vial.json` 里存在 `1.0499999999999998` 这类算出来的
宽度，不量化会与 `1.05` 算成两个几何体。

### `vial.json` 给不了的信息

只有平面位置和旋转，没有键帽 profile、没有外壳高度 / 倾角 / 脚垫。这些一律不编造。

---

## 二、进展

### 已完成

**步骤 1 · 字符与键帽解耦**

`makeLabelTexture()` 改 `clearRect` 只绘制字形，键帽换单一 `MeshStandardMaterial`，
字符做成独立 legend 平面（对齐 `MacroKeycap3D.tsx:112` 的既有做法）。

原本 `ctx.fillStyle = "#f2f2f2"` + `fillRect` 把键帽底色烤进了纹理，导致改 `CAP_COLOR`
只有侧面变色。现已解开。

顺带修掉：旧代码把方形纹理贴到 BoxGeometry 顶面，6.25u 空格键的 UV 会把字横向拉长 6 倍。
legend 平面现在强制取**短边**做正方形，所有键字形比例一致。

**步骤 2 · 几何体与材质缓存**

几何体按 `cap:${w}x${h}` / `legend:${size}` / `knob:${r}` 缓存，材质全局共享，纹理按 label
缓存。重建 effect 只分配 Mesh 壳子，dispose 集中到卸载时一次。

实施中发现的连带改动：材质共享后，原先靠改 `emissive` 实现的 hover 会**让整块板子一起发光**。
改为预建 normal / hover 两个共享材质、交换 `mesh.material` 引用。另修掉一个悬空引用——
重建时 `state.hovered` 仍指向已移出场景的 mesh。

**步骤 3 · 键帽造型**

`RoundedBoxGeometry` + `taperCap()` 按高度线性缩放顶点做上窄下宽，之后
`computeVertexNormals()`。编码器旋钮同样加了锥度。

**步骤 4 · 灯光与材质**

主光 1.35 + 反向补光 0.35，`PCFSoftShadowMap`，阴影贴图 2048。阴影相机按板子尺寸设置
（默认 ±5 的正交范围会裁掉大于 40% 配列的阴影）。

**步骤 5 · 外壳形状：按键边界生成**

原本用 `placed.width × placed.height`（整体轴对齐包围盒）画一块平板，对两类键盘是错的：
分体键盘得到一整块中间空着的大板子；带旋转簇的配列（Alice）包围盒远大于实际键区。

新增 `src/components/keymap/caseOutline.ts`（纯函数、不依赖 three、有测试）：

- `clusterHulls()` — 并查集按外扩包围盒重叠分簇（`CLUSTER_GAP = 0.8` KLE 单位），
  每簇一个 Andrew 单调链凸包，保证 CCW 绕向。分体键盘返回两个凸包。
- `offsetOutline()` — 沿边外法线偏移，顶点用圆弧替代尖角（与圆盘的 Minkowski 和），
  圆角是免费的。退化输入（1 点 / 共线）分别得到圆和胶囊。

外壳放进独立的 group 和 effect，只在 `[keyboard, layoutKey]` 变化时重算——凸包 + 挤出的
开销放在每次改键上不可接受。外壳几何体每套布局唯一，不进缓存，用 `state.caseGeo` 单独跟踪销毁。

坐标映射注意：`ExtrudeGeometry` 在 XY 平面挤 +Z，摆平需 `rotation.x = -PI/2`，这会把
shape 的 y 映到世界 **-Z**，建轮廓时 z 要取负。

### 进行中

**步骤 6 · 外壳形态：从「两片薄板」改成真正的壳体**

步骤 5 只解决了外壳的**平面轮廓**，没解决**侧面形态**。实测截图（侧视）暴露的问题：

- 外壳是两片各 0.12 厚的薄板叠在一起，看起来像两张纸，不像有体积的壳体；
- 键帽底面完全暴露在外，与板面之间还有 `CAP_INSET` 的可见空隙 —— 键帽是「浮」在板上的，
  不是「坐」进壳里的。

真实键盘侧视应该是：壳体有实在的高度，四周有一圈边框（bezel）高出键帽底面，键帽从凹进去的
板面升起。改法：

1. **壳体（body）**：外轮廓实心挤出，从 `PLATE_TOP - CASE_HEIGHT` 到 `PLATE_TOP`，
   顶面即凹陷的板面；
2. **边框（bezel）**：外轮廓 + 内孔的环形挤出，从 `PLATE_TOP` 升到 `BEZEL_TOP`
   （高于键帽底面），遮住键帽底部和那道空隙。

内外两条轮廓都是同一个凸包用不同 margin 走 `offsetOutline()` 得到的，无需新算法；
环形靠 `THREE.Shape.holes` 实现（内孔需反向绕向）。

---

## 三、明确不做

| 项 | 原因 |
| --- | --- |
| R1–R4 行高差 / 倾角 | 配列千奇百怪，行语义推不出来，见「一」 |
| 真实 Cherry/SA 曲面 profile | 与等高前提冲突，且成本跳到数天 |
| 顶面凹陷（dish） | 需加细分再位移顶点，收益低 |
| 环境贴图反射 / AO | 纯观感，投入产出比低 |
| 外壳倾角 / 脚垫 / 真实高度 | `vial.json` 无数据，只能编造 |
| 3D 页内的改键交互 | 该页定位为纯展示，改键留在首页 2D 视图 |
| 凸包换 marching squares | 见下「已知取舍」，等实测觉得糊了再说 |

## 四、已知取舍与遗留

- **凸包会填平凹口**：人体工学键盘拇指簇与主键区之间的凹陷会被抹平。真实外壳大多是凸的，
  暂时可接受。升级路径是把 footprint 光栅化到占据栅格再用 marching squares 提轮廓——
  分簇逻辑可继续复用，只替换凸包这一步。
- **`CLUSTER_GAP = 0.8` 是估值**：分体两半间距小于此值会被并成一块；常规板子内部若有大于
  此值的空隙（某些 1800 配列的功能区间隔）会被误拆。需实测调整。
- **3D 视图无选中态高亮**：组件只有 hover 的 emissive 效果，没有 `selected` prop。
  当前页面为纯展示，暂无需求。
- **键帽仍有 `cursor: pointer` 和 hover 高亮但点击无响应**，存在「看起来能点」的误导。
  如需消除可加 `interactive={false}` 一并关闭。
- **2D 预览有同样的外壳问题**：`.keyboard-case`（`index.css:516`）是单个 div 套住整个
  包围盒，分体 / 异形同样错。已另派子 Agent 处理，复用 `caseOutline.ts`。

## 五、验收

- `pnpm build` 通过（`tsc -b` 严格模式为主要正确性门禁）；
- `pnpm test` 通过；
- 与首页 2D 视图对比：键位、旋转、布局选项、大键 / ISO Enter 的位置一致
  （两者共用 `layoutGeometry.ts` 的 `placeLayout()`，本次不改该文件）；
- 改单个键位时无可见卡顿。
