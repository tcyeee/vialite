
# 自制键盘配置器可行性分析

> 本文属于嵌入式开发与硬件外设领域,与 [嵌入式开发](src/202605121228-嵌入式开发.md)、[底层硬件](src/202604111600-底层硬件.md)、[我的客制化键盘](src/202607091228-我的客制化键盘.md) 同属该体系。

## 背景

配置[我的客制化键盘](src/202607091228-我的客制化键盘.md)(牛牛 AT40)用的是 Vial 官方网页版 vial.rocks,交互设计繁琐,布局编辑、宏录制、层级切换等操作路径不符合个人习惯。目标已经明确为三点:

1. 不只服务自己的 AT40,而是做一个适配所有 Vial 协议键盘的通用配置器,面向客制化圈子分发。
2. 用 Web 实现。
3. 在已有项目基础上继续构建,目标只是做出更好的交互,不重新发明协议层。

## Vial 的技术架构

Vial 项目完全开源,分为三部分:

- **vial-qmk**:QMK 固件的分支,负责在键盘侧实现协议响应,并把键盘的布局定义(按键矩阵、层数、旋钮编码器等)以 `vial.json` 的形式编译进固件。
- **vial-gui**:官方桌面客户端,用 Python 加 PyQt 实现,通过 hidapi 与键盘通信。
- **vial-web**:vial-kb 组织下的仓库,大概率就是 vial.rocks 背后的源码,基于浏览器的 WebHID API 实现,不需要安装驱动。

关键点是:`vial.json` 由每块键盘的固件在运行时提供,GUI 不需要预先知道键盘型号,而是连接后现场读取布局定义。这意味着协议本身天生就是跨键盘型号的,vial-web today 已经能配置任意一款刷了 vial-qmk 的键盘,不是专门为某一款硬件写死的。这一点直接决定了"通用配置器"这个目标从协议层面看是现成的,不需要额外开发。

```mermaid
flowchart LR
    subgraph 浏览器
        UI[配置器前端 UI]
        HID[WebHID API]
    end
    UI --> HID
    HID <-->|USB Raw HID 报文| FW["任意 vial-qmk 键盘"]
    FW -->|运行时读取| VJSON[键盘自带的 vial.json 布局定义]
```

## 路线:fork vial-web

三点目标里已经定了方向,不需要再对比"自研协议"这条路。选择 fork vial-web 而不是从零写的理由:

- 协议通信、`vial.json` 解析、宏/组合键/敲击舞等复杂功能的数据模型,vial-web 已经跑通并被社区验证过,重写风险高、收益低。
- 需要改的只是交互层:按键选择方式、层级切换动线、常用操作的可发现性等,这些都在 UI 组件和状态管理层面,不涉及协议。
- 保留对上游的兼容,后续 vial-qmk 协议如果新增字段或键盘固件更新,能更容易跟进上游的适配。

## 开源协议与合规

vial-gui、vial-qmk 都是 GPL-2.0 许可,vial-web 大概率延续同一许可(以仓库实际 LICENSE 文件为准,fork 前需要确认)。GPL-2.0 是 copyleft 协议,意味着:

- fork 之后的项目也必须保持开源,不能做成闭源产品。
- 需要保留原作者的版权声明,并说明代码基于 vial-web 修改而来。

对于"面向社区免费分发"这个目标来说,GPL 的要求本身不构成障碍,反而和开源分享的定位是一致的。

## 面向社区分发的额外考量

这是相比"只给自己用"多出来的部分,决定了工作量不能只按 AT40 一款键盘来估:

- **兼容性验证**:需要用几款有代表性的键盘(不同厂商、不同键位数、ISO/ANSI 差异)实测,确认重做的 UI 在别人的 `vial.json` 上也能正常渲染,不能只在自己的 AT40 上跑通就发布。
- **信任问题**:让别人在浏览器里对一个第三方网站授权 WebHID 访问键盘,是有心理门槛的。保持代码开源、不做任何遥测或数据上传、写清楚隐私说明,是获得社区信任的前提。
- **命名与品牌区分**:需要一个独立的名字和域名,避免被误认成官方 vial.rocks 或引起品牌混淆。
- **持续维护成本**:上游 vial-web 会持续修 bug、加新 keycode 支持。fork 出来的项目需要一个跟进上游变更的机制(定期 rebase 或关注上游 changelog),否则协议更新后容易掉队。
- **托管成本低**:WebHID 通信全部在客户端完成,不需要后端服务器,静态托管(Cloudflare Pages、Vercel、GitHub Pages 等)基本免费,这一点对面向社区免费分发是有利条件。

## 工作量拆解

1. **协议通信层**:复用 vial-web,不用重写。
2. **UI/UX 重设计**:核心工作量。建议先聚焦最高频操作(层查看、单键重映射),把这部分打磨到明显优于官方,再逐步扩展。
3. **多键盘兼容性测试**:找几款社区常见的键盘实测渲染和交互,而不是只验证自己的 AT40。
4. **进阶功能**:宏录制、组合键、敲击舞、RGB 灯效等,协议已支持,按需迁移或重做 UI。

## 风险与难点

- 不同厂商的 `vial.json` 实现质量参差不齐,个别键盘可能有非标准字段,需要用多款设备实测兜底,不能假设所有键盘都规范。
- WebHID 目前只有 Chromium 系浏览器(Chrome、Edge)支持,Safari 和 Firefox 不支持,这是面向社区分发时用户会直接遇到的限制,需要在产品说明里提前告知。
- GPL-2.0 要求保持开源,如果后续有商业化想法(比如加广告或收费功能)需要提前了解协议边界。
- 作为面向他人的工具,信任建立和长期维护是比技术实现更持久的成本。

## 结论

技术可行性高,而且"通用配置器"这个目标在协议层面几乎是免费的,因为 vial 协议本身就是运行时读取 `vial.json`、天生跨键盘型号。真正的工作量集中在 UI/UX 重做和多键盘兼容性验证上。建议推进路径:

1. Fork vial-web,先在自己的 AT40 上把构建流程和协议通信跑通。
2. 重做层查看、单键重映射这个最高频场景的交互,同时找 1 到 2 款其他社区常见键盘做兼容性验证,确保不是只服务自己。
3. 按 GPL-2.0 要求开源发布,注明基于 vial-web fork、致谢上游。
4. 收集客制化圈子的真实反馈迭代,再逐步覆盖宏、组合键、灯效等进阶功能。
5. 建立跟进上游 vial-web 更新的机制,避免协议或 keycode 支持掉队。

## 项目命名

仓库名定为 **Vialite**(Vial + lite),保留 Vial 的辨识度,同时暗示交互更轻量、更好用。检索 GitHub 和 npm 未发现同名冲突。
