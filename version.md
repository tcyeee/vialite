
> [!INFO] Title
> 页面记录[Vialite](src/202607141935-Vialite.md)的更新日志.  
> https://vialite.viii.me

## TODO

- [ ] `@随机复读的复读姬` 
	- [ ] 4.完善RGB配置
- [ ] 使用driver.js完成引导页适配
- [ ] 使用three.js完成键盘3D重建
- [ ] `@龟心四贱`
	- [ ] combo功能想同时按e和r输出ctrl+h，现在只能选到修饰键这里，括号里的键值改不了



## 更新日志

### version 0.1.10

> [!summary] 本次更新概要
> 更新时间：2026-07-20  
> 归档用户反馈：新增编码器旋钮支持、连接页浏览器兼容检测、快捷配置滚动演示动画，并集中优化预览区域自适应与按键测试页布局；新增 3D 预览（Beta），键盘外壳改为按键位轮廓生成。  
> 共 14 项功能优化（Features）、3 项问题修复（Bugfix）。

#### Features

- **编码器** 验证并支持旋钮配置 `@UNI·ユニーク`
- **个性化** 键盘字体 section 新增"媒体按键重置"开关（默认开启）：媒体键改缩写，翻页 / home / end 改图标 `@UNI·ユニーク`
- **连接页** 检测浏览器兼容性，不支持的浏览器给出提示 `@Howe`
- **连接页** 连接超过 1 秒判定为超时，显示 Debug 开关与浏览器信息 `@Howe`
- **快捷配置** 新增演示动画，提前告知用户配置区域的滚动逻辑 `@Nat3`
- **按键测试** 键盘宽度超出窗口时做适配 `@随机复读的复读姬`
- **按键测试** 键盘大小改为自适应
- **按键测试** 去掉按键区域边框，提示改用 DaisyUI 组件
- **个性化** 新增"预览区域自适应大小"开关（默认开启）；"显示尺寸"更名为"预览区域缩放"，开启自适应时随窗口宽度变化（可无限缩小，最多放大到 L 尺码）
- **键盘配置** 去掉配置设置下的预览区域缩放，改用个性化页面的规则
- **3D 预览** 新增「3D 预览」页面（Beta，纯展示）：按设备 vial.json 的布局几何实时生成 3D 键盘，支持拖拽旋转视角与滚轮缩放；键帽采用等高（DSA/XDA）造型，外壳按键位聚类生成，分体键盘渲染为两块独立外壳
- **个性化** 键盘预览新增右键菜单，可直接设为 KC_NO / KC_TRNS，与键盘配置页保持一致
- **快捷配置** 展开卡片列适配窄屏；宏面板切换页面后保留编辑状态
- **连接页** 诊断信息支持一键复制，并补充 UA 与构建时间；更新应用内浏览器（微信 / QQ 等）的提示文案

#### Bugfix

- **键盘预览** 修复宽键盘默认显示下无法完整显示的问题，自动对预览图做缩放 `@随机复读的复读姬`
- **按键测试** 修复倾斜键盘无法正常显示的问题 `@随机复读的复读姬`
- **键盘预览** 修复分体 / 异形（Alice 等）键盘的外壳绘制错误：原本按整体包围盒画一整块，分体键盘中间会出现大片空白外壳，旋转配列则四周虚胖；现改为按键位聚类分别生成轮廓

---

### version 0.1.9

> [!summary] 本次更新概要
> 更新时间：2026-07-19 21:53  
> 键盘预览新增视觉缩放与主题切换动画，上线自定义颜色选择器，优化展开卡片提示与信息面板交互。  
> 共 6 项功能优化（Features）。

#### Features

- **键盘预览** 新增视觉缩放，可放大查看当前层；切换主题时加入过渡动画
- **个性化** 新增自定义颜色选择器（ColorPicker），可自定义配色
- **快捷配置** 展开卡片提示改用统一的 cardHint 格式；信息面板增加显示延迟并调整定位
- **键盘布局** 选中按键时其余按键变暗，突出当前选中
- **快捷配置 / 基础按键** 隐藏滚动条，优化面板布局
- **底层** 解锁弹窗、矩阵测试、侧边栏统一按键映射逻辑

---

### version 0.1.8

> [!summary] 本次更新概要
> 更新时间：2026-07-18 23:12  
> 组合按键"多功能"卡片重构为"按键叠加"，完善 dualRole 按键上 / 下半部分配置交互，键盘预览与展开卡片可同时操作。  
> 共 6 项功能优化（Features）。

#### Features

- **组合按键** "多功能"卡片更名为"按键叠加"，重做图标（command / command+layers 叠加）与文案（左"修饰键"、右"长按激活层或修饰键"）
- **快捷配置** 展开下方卡片时，上方键盘预览仍可点选其他按键，且不收起卡片
- **dualRole·下半部分** 按边框区分两类：虚线 = ctrl/shift/alt/gui 四张修饰键卡（可选左右、含"设为空"）；实线 = 层按键 / 修饰键 / 退出三张卡
- **dualRole·上半部分** 选中时仅前三张功能卡可操作，其余置灰
- **TapDance / Combos** 卡片不再显示 TD-0，改为图标 + 编号（编辑态与显示态一致）
- **个性化** 新增"媒体按键美化"开关（默认开启）：媒体键改缩写，翻页 / home / end 改图标

---

### version 0.1.7

> [!summary] 本次更新概要
> 更新时间：2026-07-18 20:17  
> 快捷配置各卡片位置与命名微调，鼠标 / 多功能卡片补齐"详细设置"跳转入口。  
> 共 4 项功能优化（Features）。

#### Features

- **快捷配置** "其他"卡片更名为"更多"，展开后高度自适应
- **快捷配置·鼠标按键** 展开卡片新增"详细设置"，跳转 QMK 设置的"鼠标按键"章节
- **快捷配置·多功能** 展开卡片新增"详细设置"，跳转 QMK 设置的"轻触与长按"章节；红 / 蓝方块改为图标
- **快捷配置** 微调各展开卡片弹出位置；全局禁止文字选中

---

### version 0.1.6

> [!summary] 本次更新概要
> 更新时间：2026-07-18 18:15  
> 快捷配置新增"层按键"及配置弹窗，重构功能分类（新增"其他"、移除 Quantum），补齐图标与卡片细节。  
> 共 6 项功能优化（Features）、1 项问题修复（Bugfix）。

#### Features

- **层按键** 媒体按键下方新增"层按键"；弹窗含"层类型"（MO / TG / TT / OSL / TO / DF，各带彩色图标与 tooltip）与"层编号"（数量随键盘层数，选定类型后可选）
- **分类调整** 新增"其他"分类；移除整个 Quantum 分类；层切换去掉 6 张切层卡与多余 fn 卡，统一并入"其他"
- **键盘功能** 新增"灯光"卡（收纳灯光设置）与"键盘配置"卡（收纳自定义按键）
- **组合按键** 新增"多功能"卡（左：叠加修饰键；右：点按发键、长按激活层 / 修饰键）
- **样式** 媒体按键悬浮用 tooltip 说明作用；舞步、组合键统一用英文；键盘图标适配 Vol / Bright / Mute
- **图标** F13~F24、媒体按键、"其他"分类补齐图标

#### Bugfix

- 修复层切换"其他"卡中丢失的两个 fn 按键

---

### version 0.1.5

> [!summary] 本次更新概要
> 更新时间：2026-07-18 16:26  
> 重构基础按键页（布局置顶、精简分类、新增特殊按键区），集中优化快捷配置各功能卡片布局。  
> 共 8 项功能优化（Features）。

#### Features

- **基础按键** 键盘布局移至页面顶部；去掉级联选择器与任意键码分类；新增"配置设置"分类（含"自动选择下一个"开关）
- **基础按键** 布局下方新增"特殊按键"区（清空 / 穿透 / 任意，btn-soft 样式，"任意"弹出键码输入）；新增字母区快捷编辑
- **快捷配置·功能按键** 点击放大为竖向卡牌；F13-F24 改为 2 行 × 6 键
- **快捷配置·鼠标按键** 按键仿键盘方位排布并加鼠标图标，速度分类横向排列
- **快捷配置·媒体按键** 按键固定尺寸、去阴影、文字不换行
- **键盘预览** TD 等特殊按键更换视觉；双单词媒体键显示缩写（如 Web Home → WH）
- **个性化** 键盘预览吸顶；调整保存图片按钮
- **调试** 新增详细日志开关（后期移除）

---

### version 0.1.4

> [!summary] 本次更新概要
> 更新时间：2026-07-17 23:28  
> quantum 并入组合按键，支持输入任意键码，各页 Tab 切换加入动画，修复级联选择器与暗黑模式问题。  
> 共 5 项功能优化（Features）、4 项问题修复（Bugfix）。

#### Features

- **组合按键** quantum 并入，作为其中一个 section
- **基础按键** 新增 anyKeyCode 分类，支持输入任意键码
- **键盘布局** 右键菜单改为键码级联选择器，触发时同时选中对应按键
- **动画** 键盘布局、宏配置、个性化三页 Tab 切换加入动画
- **QMK 设置** section 右上角改为大按钮，加大 section 间距

#### Bugfix

- 修复 quantum 卡内级联选择器样式错误（背景 / 字体皆白）与层级过低被遮挡
- 修复 layer 多且窄屏时 tab 栏样式错误
- 修复 TapDance / Combo 卡视觉问题及配置时级联选择器被卡片截断
- 修复暗黑模式兼容问题

---

### version 0.1.3

> [!summary] 本次更新概要
> 更新时间：2026-07-17  
> 合并灯光与自定义为"键盘功能"，quantum 改卡片式配置，新增按层导出键盘图片。  
> 共 5 项功能优化（Features）。

#### Features

- **导航** 灯光 + 自定义合并为"键盘功能"（内部用 section 区分）；侧边栏"主页"改名"键盘布局"；"其他按键"改名"媒体按键"
- **quantum** 改为卡片显示（如修饰键点击展开全部选项）；单次修饰键并入"其他"；可识别按键补帮助图标
- **quantum** 新增 combo 卡（不可展开，提示"创建后自动生效"）
- **个性化** 新增保存当前层 / 所有层图片（所有层按每行两张拼接导出）；导出图扩展画布并加站点信息（https://vial.viii.me）
- **动画** 页面间切换加入切换动画

---

### version 0.1.2

> [!summary] 本次更新概要
> 更新时间：2026-07-17  
> 宏 / TapDance 更名为组合按键并重做卡片交互，新增层切换分类卡片，为 104 键盘引入立体质感。  
> 共 6 项功能优化（Features）。

#### Features

- **组合按键** 宏 / TapDance 更名并移到功能按键之后；新增两张卡片，展开显示全部条目；右上角显示已用 / 可用计数，悬浮显示编辑入口
- **层切换** 新增 7 张分类卡片，点击展开显示按钮；MO 卡加"常用"角标
- **功能按键** 三个分类改为 card 样式；帮助按钮适配深浅背景并提升层级
- **键盘预览** 新增右键菜单（设为 KC_NO / KC_TRNS），菜单字体放大并加图标
- **立体质感** 104 键盘加背景与边框，按键加高光 / 阴影模拟自然光；修复预览阴影被截断
- **个性化** "显示尺寸"更名"整体配置"，新增立体感开关控制高光与阴影

---

### version 0.1.1

> [!summary] 本次更新概要
> 更新时间：2026-07-16 23:09  
> 键盘布局与快捷配置的交互与布局优化，重构连接成功页与侧边栏，完善预览与状态标签。  
> 共 7 项功能优化（Features）。

#### Features

- **快捷配置** 支持读取并判定用户已有配置；层 Tab / 分类 Tab 宽度自适应；去掉高级按钮
- **功能按键** Fn / 媒体 / 鼠标合并为"功能按键"，基础改名"基础按键"；F13-F24 排 3×4，鼠标独立成组
- **帮助** quantum 与层切换各分类加帮助按钮，悬浮显示说明
- **宏 / TapDance** 两页合并
- **连接页** 更新 banner；重构连接成功页，新增左侧贴窗菜单，窄屏自动切换
- **键盘预览** dualRole 下半部分按 macOS 模式显示修饰键规则（如 RGui → 右向 + command 图标）；图标类按键字体缩小 15%
- **状态标签** TapDance 卡按是否启用显示已用（绿）/ 未用（红）；combo 卡统一显示已生效（绿）

---

### version 0.1.0

> [!summary] 本次更新概要
> 更新时间：2026-07-14 23:30  
> 首个上线版本：基于 WebHID 的 Vial 协议配置器基础能力。  
> 共 5 项功能优化（Features）。

#### Features

- **连接** WebHID 连接 Vial 键盘，读取设备定义与各层键位
- **改键** 支持键位 / 编码器改键
- **配置** .vil 配置文件导入 / 导出
- **工具** 矩阵测试（含 Vial 解锁流程）
- **界面** 中英文切换；编写项目发布日志

